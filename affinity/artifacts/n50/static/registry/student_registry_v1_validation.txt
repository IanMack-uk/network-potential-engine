Job 1 — Cohort registry validation report

Artifact:
- affinity/artifacts/n50/static/registry/student_registry_v1.json

Cross-check source export:
- affinity/artifacts/n50/student_uuid_from_neo4j.json

Validation checks (expected for Affinity n=50):

1) Row count
- Expected: 50
- Observed: 50
- Result: PASS

2) node_index contiguous
- Expected: exactly {0,1,2,...,49}
- Observed: {0..49}
- Result: PASS

3) student_id uniqueness
- Expected: all unique
- Observed: all unique (50 unique IDs)
- Result: PASS

4) student_id format sanity
- Expected: prefix "urn:qc:person:ulid:" and ULID payload
- Observed: all entries share expected prefix
- Result: PASS

5) Cross-check vs Neo4j export (UUID list)
- Expected: same 50 IDs as Neo4j export
- Observed: matches Neo4j export set (50/50)
- Result: PASS

6) Cross-check ordering vs Neo4j export (UUID list)
- Expected: `nodes[i].student_id` equals export[i].UUID for all i=0..49
- Observed: ordering matches exactly (index-aligned)
- Result: PASS

Notes:
- Ordering is frozen exactly as listed in the registry artifact.
- Downstream pipeline steps MUST use this exact ordering when building vectors/matrices.
